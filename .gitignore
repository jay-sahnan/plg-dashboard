# dependencies
/node_modules

# next.js
/.next
/out

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# typescript
next-env.d.ts
*.tsbuildinfo

# pnpm (this project uses npm)
pnpm-lock.yaml

# user-set goals (written by the Settings page)
goals.json

# AI-assigned timeline categories (written by /api/categorize)
categories.json

# DX-audit scratch copies
dx-audit-tmp

# agent / skill tooling + scratch (not part of the template)
.agents/
.context/
skills-lock.json
agent-experience-*.html
.claude/skills/
.claude/settings.local.json
